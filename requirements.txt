# Numerical core (required for tests/test_units.py and tests/test_synthetic.py)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0

# I/O boundary (required for the real ERA5 + CEMS run)
xarray>=2023.1
netCDF4>=1.6
# open_mfdataset opens files chunked, which xarray delegates to dask. Without
# it every multi-year read raises ImportError, and the lazy coarsening in
# open_z500 - which is what keeps native 0.25 deg Z500 out of RAM - has nothing
# to defer through.
dask>=2023.1

# CDS retrieval (Stage 3 onward; needs ~/.cdsapirc)
cdsapi>=0.7

# Composite maps (--step maps). Optional: nothing in the numerical core or in
# tests/test_units.py imports either, so the pipeline and its invariants run
# without them. cartopy pulls in PROJ and shapely and is by far the heaviest
# dependency here, which is why plots.py is a separate module imported inside
# the step rather than at package level.
matplotlib>=3.7
cartopy>=0.22
